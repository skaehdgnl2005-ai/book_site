// Site footer (Atelier Sans). Quiet: wordmark + tagline, hairline top border.
export function Footer() {
  return (
    <footer className="site-footer">
      <span>그림책 제작소</span>
      <span>한 아이의 이름으로 시작되는 이야기</span>
    </footer>
  );
}
